{
  "name": "@kraftverk/ui",
  "version": "0.1.0",
  "private": true,
  "description": "The shared interface primitives: cards, rows, selectors, schema-driven forms and formatting. Used by the app shell and by device packages, so a device can bring its own screens without depending on the app.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kraftverk/plugin-sdk": "*",
    "@kraftverk/protocol": "*"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*",
    "tamagui": "*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
